server:
  port: 8080

spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT-6
  data:
    mongodb:
      # no user name and password need to be specified for now
      uri: mongodb://192.168.0.165:2020/test
